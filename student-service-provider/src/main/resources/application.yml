spring:
  application:
   name: student-service-provider

server:
  port: 9800

eureka:
   client:
     register-with-eureka: true
     fetch-registry: true
     serviceUrl:
       defaultZone: http://localhost:8761/eureka